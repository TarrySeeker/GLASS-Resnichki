import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // В репозитории несколько lock-файлов, и Next выбирает корнем родительскую
  // папку. Указываем явно, иначе трассировка файлов уходит мимо проекта.
  outputFileTracingRoot: __dirname,

  images: {
    /*
      Срок жизни оптимизированной картинки в кэше.
      По умолчанию он короткий, и после его истечения сервер пересобирает
      файл заново — на холодном кэше это 100–400 мс на каждую картинку, и
      посетитель видит пустые плитки. Съёмка бренда не меняется: когда файл
      заменят, сменится и сборка, а с ней и ключ кэша. Поэтому месяц.
    */
    minimumCacheTTL: 2678400,

    /*
      Размеры, которые сервер вообще может отдать. Из стандартного набора
      убраны 16–96 px (таких картинок на витрине нет) и 2048–3840 px
      (исходники не крупнее 1200). Каждый лишний размер — это отдельная
      пересборка файла при первом обращении.
    */
    imageSizes: [128, 256, 384],
    deviceSizes: [640, 750, 828, 1080, 1200, 1920],
  },
}

export default nextConfig
